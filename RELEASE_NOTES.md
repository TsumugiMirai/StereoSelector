# StereoSelector 发布说明

## 1.2.0（2026-09-07）

本版本把产品收敛为纯查看与测量工作台，并修正测量链路的坐标系和单位处理。

### 产品范围

- 移除样本审核、接受/拒绝、缺陷标签、审核 JSON 与 `_select` 输出目录。打开、浏览和关闭项目不再写入任何文件。
- 顶部检查工具栏集中双目快捷操作；活动栏按“数据、视图、显示调整、测量与选择、统计信息”组织。

### 测量正确性

- 标定文件识别厂商 `camera_rectify` / `camera_rectify_air` 段：深度图使用校正后的内参与坐标系反投影，不再把原始左目内参套到校正深度上。
- 同时含空气与水下（`uw`）两套校正参数时，检查栏出现“空气 / 水下”切换，默认空气，选择按项目记忆；水下项目切到“水下”后深度反投影、点云投影和校正显示统一使用水下参数。
- 支持读取与 JSON 同目录的 `map_left_*.pfm` / `map_right_*.pfm` 重映射表；加载时与 JSON 校验并自动识别上下翻转与所属模式，校正显示在对应模式下直接使用设备自身的映射，否则回退到 OpenCV `stereoRectify`。
- 深度单位由设置页显式指定（默认 uint16 视为毫米），光标 XYZ、线段长度、点云外参统一换算为米，不再按数值大小猜测单位。
- 线段测量对两端分别采样深度，并报告两端深度；饱和值 65535 在光标 XYZ 与测量中处理一致。
- 打开项目时自动发现随采集存放的标定文件（`calib/` 目录），列为“项目内”选项并在未选择标定时自动启用。

### 界面与使用

- 空状态页列出最近打开的项目，可一键重新打开。
- 检查器“导出”新增“导出调整后图像”，按原始分辨率应用亮度、对比度、Gamma、LUT 等调整后保存；原“导出当前视图”仍为屏幕截图。
- 标定分辨率与图像不一致时在状态栏与标定提示中提示一次。
- macOS / Linux 下无边框窗口支持从边缘拖动缩放；字体按平台回退，不再硬编码微软雅黑。
- 扫描项目期间禁止再次打开或拖入项目，项目集合识别移入后台线程，进度框覆盖整个过程。

### 工程

- 内置标定 JSON 随包分发（`stereo_selector/assets/calibration`），`pip install` 后仍可用。
- 版本号只在 `pyproject.toml` 维护；Windows 版本资源由打包脚本生成。
- Windows 打包刷新版本元数据，隔离外部工具的旧系统运行库，并在 CI 中验证 EXE 启动及数据加载。
- 新增 `ruff` 与 pre-commit 配置，CI 运行静态检查。
- 应用日志写入用户数据目录（Windows：`%LOCALAPPDATA%\ToolBox\StereoSelector\logs`），可用 `--verbose` 打开调试级别。
- 新增 `scripts/validate_datasets.py`，对真实采集数据做离屏功能验证（加载、标定、校正、读数、点云、导出）。

## 1.1.0

- 内置 `libra2000`、`libra3000` 两套相机标定，可在主页面快速切换。
- 支持导入 JSON、YAML 和 OpenCV YAML 标定，显示极线并计算光标位置的相机坐标。
- 新增同步十字光标、RGB/深度/XYZ 联动、左右图透明叠加以及图片同步缩放和平移。
- 新增深度图无效值、零值、空洞、极大值比例和有效范围分析。
- 自动识别非标准数据目录，也可手动映射视图并强制按自然排序匹配。
- 修正点云初始相机方向，使其与左目 RGB 拍摄方向一致。
- 优化启动提示、后台加载、缓存和快速切换，降低图片与点云闪烁。

下载 `StereoSelector-v1.1.exe` 后可直接运行，无需单独安装 Python。此版本未使用商业代码签名证书，Windows 首次运行时可能显示 SmartScreen 提示。
