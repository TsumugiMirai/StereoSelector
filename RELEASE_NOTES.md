# StereoSelector v1.0

首个正式版本，面向双目 RGB、深度图、伪彩图与 PLY 点云的人工同步筛选工作流。

## 主要功能

- 自动识别 `left`、`right`、`depth_fsd`、`depth_color`、`ply` 及常见别名目录。
- 支持按文件名匹配，也可手动指定视图目录并按自然排序强制对应。
- 支持同时选择 0–5 种视图，提供单视图平铺、聚焦、缩放与平移。
- 内置 PLY 点云显示，默认方向与双目相机拍摄方向一致。
- 图片和点云异步加载，包含延迟加载提示、最近内容缓存与快速翻页任务取消。
- 通过方向键浏览、回车接受；接受的整组文件原子复制到 `<项目名>_select`。
- 提供深色和浅色主题、可配置按钮文字、快捷键和点云显示上限。
- Windows 无边框界面支持原生四边/四角缩放、窗口吸附和集成窗口控制按钮。

## 验证

- 22 项自动化测试通过。
- 使用 50 组、5 种视图的完整测试数据验证自动匹配与强制顺序匹配。
- 验证快速连续翻页、点云切换、后台任务退出及接受文件完整性。

下载 `StereoSelector-v1.0.exe` 后可直接运行，无需单独安装 Python。

## 文件校验

- SHA-256：`DD9E7A0947148236BEDBD609A3AB3A91B70252A0E472AE04CBADC9F5A737D4BF`
- 文件版本：`1.0.0.0`

此版本暂未使用商业代码签名证书。Windows 首次运行时可能显示 SmartScreen 提示，请核对以上 SHA-256 后再选择运行。
