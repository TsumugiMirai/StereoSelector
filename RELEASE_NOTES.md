# StereoSelector v1.1

本版本扩展了标定、审核记录和多视图检查能力，并重构了主界面及设置体验。

## 主要更新

- 内置 `libra2000`、`libra3000` 两套相机标定，可在主页面快速切换。
- 支持导入 JSON、YAML 和 OpenCV YAML 标定，显示极线并计算光标位置的相机坐标。
- 新增同步十字光标、RGB/深度/XYZ 联动、左右图透明叠加以及图片同步缩放和平移。
- 新增深度图无效值、零值、空洞、极大值比例和有效范围分析。
- 输出路径和文件夹名称可自定义；审核状态、缺陷标签及文字备注保存至 JSON。
- 自动识别非标准数据目录，也可手动映射视图并强制按自然排序匹配。
- 设置页融入主窗口；标定选择、字体、关闭按钮、浅色/深色主题和弹窗阴影统一。
- 已接受样本使用整条绿色审阅栏，提高状态识别度。
- 修正点云初始相机方向，使其与左目 RGB 拍摄方向一致。
- 优化启动提示、后台加载、缓存和快速切换，降低图片与点云闪烁。

## 验证

- 48 项自动化测试通过。
- 源代码及打包后的 EXE 均通过 `test2` 的 50 组、5 种视图烟雾测试。
- 已验证两个内置标定 JSON 包含在单文件 EXE 中。

下载 `StereoSelector-v1.1.exe` 后可直接运行，无需单独安装 Python。

## 文件校验

- SHA-256：`694C239F32594C91D21D1D4FA95A7CE75166E6D3372B879D887240B4ED7A5D86`
- 文件版本：`1.1.0.0`

此版本暂未使用商业代码签名证书。Windows 首次运行时可能显示 SmartScreen 提示，请在运行前核对 Release 页面中的 SHA-256。
