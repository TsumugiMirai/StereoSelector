# Dependencies are declared once in pyproject.toml. This file only exists for
# tools that expect a requirements file; it installs the project in editable
# mode together with the build and test extras.
-e .[build,test]